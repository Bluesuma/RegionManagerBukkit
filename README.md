# RegionManagerBukkit

⚠️ **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ v1.0.4** ⚠️

## 🚀 НОВЫЕ ВОЗМОЖНОСТИ в версии 1.0.4:

### 🎯 Система предиктов движения игроков
- **Предсказание направления**: Анализирует движение игроков и предугадывает их будущие позиции
- **Адаптивное расширение регионов**: Автоматически расширяет регионы при приближении игроков
- **Предварительная загрузка**: Загружает регионы до входа игрока для плавного перехода
- **Умная оптимизация**: Уменьшает размеры регионов для неактивных игроков

### ⚡ Быстрая выгрузка пустых регионов
- **Немедленная выгрузка**: Пустые регионы выгружаются мгновенно (кроме принудительно созданных)
- **Настраиваемая задержка**: Возможность установить задержку выгрузки через конфигурацию
- **Принудительные регионы**: Флаг для регионов, которые не должны выгружаться автоматически

### 🔧 Новые команды администратора
- `/region force [игрок]` - Создать принудительный регион (не выгружается автоматически)
- `/region predict [игрок]` - Показать предсказания движения игрока
- `/region unload <регион>` - Принудительно выгрузить регион

### ⚙️ Расширенная конфигурация
- **Настройки предиктов**: Расстояние предсказания, время, пороги скорости
- **Адаптивное управление**: Минимальный/максимальный размер регионов
- **Быстрая выгрузка**: Настройки для немедленной выгрузки пустых регионов

---

## 🔧 Исправления в версии 1.0.3:

### ✅ Исправлено создание новых регионов при телепортации
- **Проблема**: При телепортации на большие расстояния новые регионы не создавались
- **Решение**: 
  - Добавлена проверка расстояния до центра региона (максимум половина размера региона)
  - Улучшена логика `findOrCreateRegionForPlayer` с детальным логированием
  - Принудительное создание нового региона при телепортации
  - Добавлена команда `/region create [игрок]` для тестирования
- **Результат**: Плагин корректно создает новые регионы при телепортации игроков

### ✅ Улучшена логика поиска центров регионов
- **Проблема**: Новые регионы создавались слишком близко к существующим
- **Решение**: 
  - Улучшен алгоритм `findOptimalRegionCenter` с проверкой расстояний
  - Добавлены множественные варианты смещения для поиска подходящего места
  - Детальное логирование процесса создания регионов
- **Результат**: Регионы создаются с правильными интервалами

### ✅ Добавлена команда для тестирования
- **Новая команда**: `/region create [игрок]` - принудительно создать регион для игрока
- **Использование**: Позволяет администраторам тестировать создание регионов
- **Права**: Требует разрешение `regionmanager.admin`

---

## 🔧 Исправления в версии 1.0.2:

### ✅ Исправлен расчет TPS
- **Проблема**: Плагин показывал неправильный TPS (0.20 вместо реального значения)
- **Решение**: Использован правильный метод `Bukkit.getTPS()` для получения точных данных о производительности сервера
- **Результат**: Теперь плагин корректно отображает TPS: 20.0, 19.8, 18.5 и т.д.

### ✅ Исправлено создание новых регионов
- **Проблема**: Новые регионы не создавались когда игрок выходил за пределы существующего
- **Решение**: 
  - Улучшена логика проверки смены региона
  - Добавлена настройка `regions.check-distance: 64` для контроля частоты проверки
  - Регионы теперь создаются автоматически при выходе игрока за границы
- **Результат**: Плагин корректно создает новые регионы при перемещении игроков

### ✅ Улучшена конфигурация
- Добавлены новые настройки для точного контроля создания регионов
- Улучшено логирование операций с регионами
- Добавлены настройки для отладки

---

## 📋 Описание

RegionManagerBukkit - это плагин для PaperMC/Purpur, который оптимизирует производительность сервера путем распределения игроков по регионам. Плагин автоматически управляет загрузкой и выгрузкой областей мира, что помогает поддерживать стабильный TPS 20.

## 🚀 Основные возможности

- **Автоматическое распределение игроков** по регионам
- **Оптимизация TPS** через управление загрузкой чанков
- **Асинхронная загрузка** чанков для предотвращения лагов
- **Автоматическая выгрузка** неиспользуемых регионов
- **Мониторинг производительности** в реальном времени
- **Гибкая конфигурация** для разных типов серверов
- **Команды администратора** для управления регионами

## 📦 Установка

1. Скачайте последнюю версию плагина
2. Поместите JAR файл в папку `plugins/`
3. Перезапустите сервер
4. Настройте конфигурацию в `plugins/RegionManagerBukkit/config.yml`

## ⚙️ Конфигурация

### Основные настройки регионов:
```yaml
regions:
  size: 512                    # Размер региона в блоках
  max-players-per-region: 20   # Максимум игроков в регионе
  check-distance: 64          # Расстояние проверки смены региона
  auto-create: true           # Автоматическое создание регионов
```

### Настройки производительности:
```yaml
performance:
  target-tps: 20.0            # Целевой TPS
  min-tps: 15.0               # Минимальный TPS для экстренной оптимизации
  tps-check-interval: 100     # Интервал проверки TPS
```

### Настройки системы предиктов:
```yaml
prediction:
  enabled: true               # Включить/выключить систему предиктов
  distance: 128               # Расстояние предсказания движения (в блоках)
  time-seconds: 10            # Время предсказания движения (в секундах)
  speed-threshold: 0.1        # Порог скорости для активации предсказания
  preload-distance: 64        # Расстояние для предварительного расширения
```

### Настройки адаптивного управления:
```yaml
regions:
  size: 512                   # Базовый размер региона
  min-size: 256               # Минимальный размер региона
  max-size: 1024              # Максимальный размер региона
  fast-unload: true           # Быстрая выгрузка пустых регионов
  unload-delay-ticks: 600     # Задержка выгрузки (в тиках)
```

### Настройки загрузки чанков:
```yaml
chunk-loading:
  max-view-distance: 4        # Максимальный радиус загрузки
  chunk-load-delay: 10        # Задержка между загрузкой (мс)
  async-chunk-loading: true   # Асинхронная загрузка
  max-chunks-per-batch: 16    # Максимум чанков в партии
```

## 🎯 Рекомендуемые настройки загрузки чанков

### Для слабых серверов (1-5 игроков):
```yaml
chunk-loading:
  max-view-distance: 2
  chunk-load-delay: 20
  max-chunks-per-batch: 8
```

### Для средних серверов (5-20 игроков):
```yaml
chunk-loading:
  max-view-distance: 4
  chunk-load-delay: 10
  max-chunks-per-batch: 16
```

### Для мощных серверов (20+ игроков):
```yaml
chunk-loading:
  max-view-distance: 6
  chunk-load-delay: 5
  max-chunks-per-batch: 32
```

## 🔧 Команды

- `/region info` - Информация о текущем регионе
- `/region list` - Список всех активных регионов
- `/region optimize` - Принудительная оптимизация регионов
- `/region stats` - Статистика производительности
- `/region create [игрок]` - Принудительно создать регион для игрока
- `/region force [игрок]` - Создать принудительный регион (не выгружается автоматически)
- `/region predict [игрок]` - Показать предсказания движения игрока
- `/region unload <регион>` - Принудительно выгрузить регион

## 📊 Принцип работы регионов

### Создание регионов:
1. При входе игрока создается регион вокруг его позиции
2. Размер региона определяется настройкой `regions.size`
3. Регион автоматически загружает чанки в радиусе `chunk-loading.max-view-distance`

### Распределение игроков:
1. Игроки автоматически распределяются по ближайшим регионам
2. При достижении лимита игроков создается новый регион
3. Регионы объединяются при сближении игроков

### 🎯 Система предиктов движения:
1. **Анализ движения**: Отслеживает скорость и направление движения игроков
2. **Предсказание позиции**: Вычисляет будущую позицию игрока через `prediction.time-seconds` секунд
3. **Адаптивное расширение**: Расширяет регионы при приближении игроков к границам
4. **Предварительная загрузка**: Загружает целевые регионы до входа игрока
5. **Умная оптимизация**: Уменьшает размеры регионов для неактивных игроков

### ⚡ Быстрая выгрузка:
1. **Немедленная выгрузка**: Пустые регионы выгружаются мгновенно при `regions.fast-unload: true`
2. **Принудительные регионы**: Регионы с флагом `isForcedRegion` не выгружаются автоматически
3. **Настраиваемая задержка**: Возможность установить задержку через `regions.unload-delay-ticks`

### Асинхронная загрузка:
1. Чанки загружаются в отдельном потоке
2. Загрузка происходит партиями для контроля нагрузки
3. Настраиваемые задержки предотвращают перегрузку сервера

## 📈 Производительность

### Мониторинг:
- Отслеживание TPS в реальном времени
- Мониторинг количества активных регионов
- Статистика загрузки чанков

### Оптимизация:
- Автоматическая выгрузка пустых регионов
- Принудительная оптимизация при низком TPS
- Управление памятью через сборку мусора

### Рекомендации:
- **Радиус загрузки чанков**: Уменьшите при низком TPS
- **Размер региона**: Увеличьте для лучшей производительности
- **Интервал проверки**: Настройте под нагрузку сервера

## 🛠️ Устранение проблем

### Зависание сервера при загрузке чанков:
1. Уменьшите `chunk-loading.max-view-distance` до 2-3
2. Увеличьте `chunk-loading.chunk-load-delay` до 20-50
3. Уменьшите `chunk-loading.max-chunks-per-batch` до 8-12
4. Убедитесь что `chunk-loading.async-chunk-loading: true`

### Низкий TPS:
1. Проверьте настройки `performance.min-tps`
2. Уменьшите размер регионов (`regions.size`)
3. Включите `performance.force-unload-on-low-tps: true`
4. Проверьте логи на наличие ошибок

### Не создаются новые регионы:
1. Убедитесь что `regions.auto-create: true`
2. Уменьшите `regions.check-distance` до 32-64
3. Проверьте логи на ошибки создания регионов
4. Включите `debug.show-region-info: true` для отладки

### Проблемы с памятью:
1. Уменьшите `regions.size`
2. Включите автоматическую выгрузку регионов
3. Проверьте настройки JVM для сервера
4. Мониторьте использование памяти через команды

## 📝 Логирование

### Уровни логирования:
- `INFO` - Основная информация о работе плагина
- `DEBUG` - Детальная отладочная информация
- `WARN` - Предупреждения о проблемах
- `ERROR` - Критические ошибки

### Типы логов:
- `region-operations` - Операции с регионами
- `performance-metrics` - Метрики производительности
- `chunk-loading` - Загрузка чанков
- `player-movements` - Перемещения игроков

## 🔄 Обновления

### v1.0.4 (Текущая версия):
- 🎯 Добавлена система предиктов движения игроков
- ⚡ Реализована быстрая выгрузка пустых регионов
- 🔧 Добавлены новые команды администратора
- ⚙️ Расширена конфигурация для адаптивного управления
- 🚀 Улучшена производительность и отзывчивость

### v1.0.3:
- ✅ Исправлено создание новых регионов при телепортации
- ✅ Улучшена логика поиска центров регионов
- ✅ Добавлена команда `/region create` для тестирования
- ✅ Детальное логирование процесса создания регионов

### v1.0.2:
- ✅ Исправлен расчет TPS
- ✅ Исправлено создание новых регионов
- ✅ Улучшена конфигурация
- ✅ Добавлены новые настройки отладки

### v1.0.1:
- ✅ Критическое исправление зависания сервера
- ✅ Асинхронная загрузка чанков
- ✅ Настраиваемые параметры загрузки

### v1.0.0:
- 🎉 Первый релиз
- ✅ Базовая функциональность регионов
- ✅ Система мониторинга TPS

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь в актуальности версии
3. Проверьте настройки конфигурации
4. Обратитесь к разделу "Устранение проблем"

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

---

**Версия**: 1.0.4 (с системой предиктов и адаптивным управлением)  
**Совместимость**: PaperMC 1.21+, Purpur 1.21+  
**Автор**: RegionManager Team 

## 🎯 Примеры использования новых функций

### Система предиктов:
```bash
# Показать предсказания движения для игрока
/region predict PlayerName

# Создать принудительный регион (не будет выгружаться автоматически)
/region force PlayerName

# Принудительно выгрузить пустой регион
/region unload region_1
```

### Настройки для разных типов серверов:

#### Для PvP серверов (быстрое движение):
```yaml
prediction:
  distance: 256               # Большее расстояние предсказания
  time-seconds: 5             # Меньшее время предсказания
  speed-threshold: 0.05       # Более чувствительный порог

regions:
  fast-unload: true           # Быстрая выгрузка для экономии ресурсов
  unload-delay-ticks: 300     # Короткая задержка выгрузки
```

#### Для строительных серверов (медленное движение):
```yaml
prediction:
  distance: 64                # Меньшее расстояние предсказания
  time-seconds: 15            # Большее время предсказания
  speed-threshold: 0.2        # Менее чувствительный порог

regions:
  fast-unload: false          # Медленная выгрузка для стабильности
  unload-delay-ticks: 1200    # Длинная задержка выгрузки
``` 